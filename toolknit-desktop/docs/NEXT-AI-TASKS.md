# Karui 下一阶段 AI 任务清单

> 规则：后续 AI 一次只领取一个任务；先看依赖，再开始改代码。完成后在任务下追加完成日期、提交号和验证结果。

## 优先顺序总览

| 顺序 | 任务 | 优先级 | 依赖 | 预计规模 |
|---|---|---:|---|---|
| 1 | T001 创作工程 schema 与样例 | P0 | 无 | 小 |
| 2 | T016 系统能力检测与运行监控 | P0 | 无 | 中 |
| 3 | T003 编辑器模块骨架 | P0 | T001 | 中 |
| 4 | T004 统一媒体探测服务 | P0 | T001 | 中 |
| 5 | T005 命令栈与撤销/重做 | P0 | T003 | 中 |
| 6 | T006 单轨时间线与片段操作 | P0 | T003/T004/T005 | 大 |
| 7 | T007 结构化 RenderPlan 与多片段导出 | P0 | T006 | 大 |
| 8 | T008 音频轨、波形与混音 | P0 | T006/T007 | 大 |
| 9 | T009 字幕轨与 SRT/VTT | P0 | T006/T007 | 中 |
| 10 | T012 本地 Whisper 字幕 | P1 | T004/T009/Job | 大 |
| 11 | T013 ComfyUI 参考图上传 | P1 | 现有 Comfy | 中 |
| 12 | T014 遮罩与参考图修图 | P1 | T013 | 大 |
| 13 | T002 工程保存/打开/自动保存 | P1 | T001/T007 | 中 |
| 14 | T015 Skill manifest 与执行预览 | P2 | T005 | 大 |
| 15 | T010 SQLite 与记忆 CRUD | P2 | T002 | 大 |
| 16 | T011 记忆导出/导入 | P2 | T010 | 中 |

---

## T016：系统能力检测与运行监控

状态：**已完成首版（2026-09-23）**

### 已实现

- 桌面端读取 CPU、逻辑线程、系统内存、GPU 型号和独立显存；
- NVIDIA 读取实时 GPU 占用、显存占用、温度和驱动版本；
- Windows 性能计数器作为 AMD/Intel 的实时负载回退；
- 按显存/内存给出 SD 1.5、SDXL、FLUX 量化版与 Wan 2.1 1.3B 的保守建议；
- AI Studio 运行期间每 2.5 秒刷新状态，页面隐藏或关闭后停止轮询；
- 浏览器预览明确提示需使用桌面端检测。

### 验证

- `npm run test:hardware-advisor`：4/4 通过；
- `npm run build`：通过；
- `cargo check --offline`：通过。

---

## T001：定义 Karui Project v1 schema 与 fixtures

状态：**已完成（2026-09-23）**

### 目标

把 [KARUI-CREATOR-ARCHITECTURE.md](./KARUI-CREATOR-ARCHITECTURE.md) 中的工程草案变成可校验、可测试的数据模型。

### 允许修改

- 新建 `src/features/editor/project-schema.js`；
- 新建 `tests/fixtures/projects/`；
- 新建最小 schema 测试脚本；
- 必要时增加轻量 schema 校验依赖，但优先评估无依赖实现。

### 不做

- 不做时间线 UI；
- 不做 FFmpeg 导出；
- 不改现有工具页面。

### 验收

- 能创建空工程；
- 能校验合法/非法工程；
- 对未知字段向前兼容，对未知高版本给明确错误；
- JSON 序列化再反序列化结果稳定；
- 时间、ID、asset 引用有校验；
- 至少有空工程、三片段工程、字幕工程、损坏工程四个 fixture。

### 完成记录

- 日期：2026-09-23
- 提交：尚未提交
- 实现：新增工程创建、结构校验、跨引用校验、未来版本处理、稳定序列化和解析错误；新增四个 fixture。
- 验证命令：`npm run test:project-schema`（9/9 通过）、`npm run build`。
- 已知限制：尚未实现磁盘保存、迁移器和时间线 UI；v1 暂不支持变速片段。
- 后续建议：继续 T002 工程保存、打开、自动保存与恢复。

---

## T002：工程保存、打开、自动保存和迁移

### 目标

通过 Rust command 安全读写 `.karuiproject`。

### 要求

- 原子保存：写临时文件、fsync/close、替换正式文件；
- 最近一次自动保存单独存放，不覆盖用户手动保存；
- 打开时校验 schemaVersion；
- 保存失败不修改 dirty 状态；
- 路径含中文/空格可用；
- 记录最近项目只保存路径和展示信息。

### 验收

- 新建 → 保存 → 修改 → 自动保存 → 模拟退出 → 恢复成功；
- 写入中断不会破坏旧工程；
- 无权限路径有中文可理解错误；
- 旧 schema fixture 可迁移。

---

## T003：独立编辑器模块骨架

**状态：2026-09-24 已完成骨架验收。** 已提供独立入口、四区工作台、默认 V1/A1/S1 轨道、素材导入和 Karui 动态氛围；真实片段编辑由 T004-T006 继续实现。

### 目标

建立独立页面和模块边界，停止向 `main.js` 追加编辑器逻辑。

### 布局

- 左：素材库/项目；
- 中：播放器；
- 右：检查器/AI 助手；
- 下：时间线；
- 顶部：工程、撤销/重做、导出。

### 验收

- 能从视频分类进入和退出；
- 不影响现有视频转换工具；
- 空工程 UI、未保存状态、窗口缩放正确；
- UI 使用现有 Karui token、动态背景只做弱化氛围，不影响时间线可读性；
- 新逻辑全部位于 `src/features/editor/`。

---

## T004：统一媒体探测服务

**状态：2026-09-24 已完成首版。** Rust 已提供结构化 `probe_media`，编辑器导入后会自动分析并展示核心参数；无音轨、VFR 和手机旋转视频的二进制回归 fixture 仍需后续补齐。

### 目标

把现有 `probe_video` 扩展为稳定的 `probe_media`。

### 输出

- container、duration、size；
- video streams：codec、width、height、fps rational、rotation、pixFmt、color；
- audio streams：codec、sampleRate、channels、layout、language；
- subtitle streams；
- 文件 fingerprint；
- 结构化错误。

### 验收

- MP4/MOV/MKV/MP3/WAV/JPG/PNG 可探测；
- 无音轨、可变帧率、手机旋转视频有 fixture；
- ffprobe 输出异常不会 panic；
- 前端不解析原始 ffprobe JSON。

---

## T005：Command Bus 与撤销/重做

### 目标

实现可测试的编辑命令系统。

### 验收

- insert/move/trim/split/delete 可执行和撤销；
- redo 可用；
- 连续拖动合并为一个历史项；
- 一组 AI 操作为单个 transaction；
- 失败命令不污染状态；
- 至少 20 个纯数据单元测试。

---

## T006：单视频轨时间线

### 目标

支持多个片段的排序、裁剪、分割和预览定位。

### 验收

- 拖入 3 个素材；
- 播放头与时间刻度同步；
- trim 不改变原素材；
- split 后 source range 正确；
- 吸附可开关；
- 缩放时间线不改变数据；
- 键盘：空格、左右逐帧、Ctrl+K、Delete、Ctrl+Z/Y；
- 100 个片段仍可交互，避免每帧重建全部 DOM。

---

## T007：多片段导出

### 目标

把工程序列编译为 FFmpeg 渲染计划。

### 验收

- 不同分辨率、帧率、旋转信息的三个片段可输出统一 MP4；
- 音画同步；
- 无音轨片段不会导致 concat 失败；
- 有进度、取消、错误详情；
- 输出使用临时文件和原子完成；
- 所有参数通过 Rust `Command::args` 传入，不拼 shell。

---

## T008：音频轨、波形和基础混音

### 目标

增加原声和 BGM，支持基础专业音频控制。

### 验收

- 至少两条音频轨；
- 波形缓存可重建；
- gain/mute/fade in/fade out；
- BGM 循环或裁剪；
- 可选响度标准化；
- 最终导出没有削波和明显不同步。

---

## T009：字幕轨

### 验收

- SRT/VTT 导入；
- 字幕块可移动、裁剪、分割和改字；
- 支持全轨样式和单条覆盖；
- 可导出 SRT/VTT；
- 可选择烧录到视频或单独文件；
- 中文字体从明确资源路径加载，缺失时提示而不是静默失败。

---

## T010：SQLite 记忆 CRUD

### 目标

把长期记忆从 `localStorage` 迁移到版本化数据库。此任务不立即迁移 API Key。

### 验收

- migration 可重复执行；
- 创建/读取/修改/软删除 memory；
- 按 scope 和 kind 查询；
- UI 能解释来源和影响范围；
- 用户可“忘记”单条或某作用域；
- 数据库损坏时有备份和恢复提示。

---

## T011：记忆导出/导入

### 验收

- 生成版本化 `.karuimemory`；
- 可选是否包含参考素材；
- checksum 校验；
- 导入前差异预览；
- 合并/覆盖/跳过冲突；
- ZIP Slip 防护；
- 扫描导出包，确认不含 `ai_api_key`、Token、Cookie、Comfy token 和本地绝对路径。

---

## T012：本地 Whisper 自动字幕

### 验收

- provider 接口，不把具体实现写死在字幕 UI；
- 模型管理有大小、版本、hash、许可证；
- 支持中文/英文、句级时间码，条件允许时逐词时间码；
- CPU 模式可用，GPU 不可用时能回退；
- 可取消和恢复；
- 生成的是内部字幕轨，不只是一个 SRT 文件。

---

## T013：ComfyUI 参考图上传

### 目标

打通参考图、待修改图和遮罩到 ComfyUI 的输入链。

### 验收

- 本地/远程 provider 均可上传；
- 文件名安全且不会路径穿越；
- 支持 image 与 mask；
- 远程上传前有隐私提示；
- 上传失败可重试；
- 工作流用返回的服务端引用，不依赖本机绝对路径。

---

## T014：参考图修图与版本链

### 验收

- 至少实现 inpaint 和 upscale 两种模板；
- 遮罩画笔、橡皮、反选、羽化、撤销；
- 运行前检测 requiredNodes/requiredModels；
- 结果不覆盖原图；
- 可对比原图/结果并选择加入素材库；
- 保存 parentAssetId、workflow、模型、seed 和提示词快照。

---

## T015：Skill manifest 与执行预览

### 验收

- manifest schema 校验；
- 权限列表可视化；
- 一个内置“口播精剪”示例，只生成编辑建议；
- 用户能查看将删除/移动/新增哪些片段和字幕；
- 确认后以单个 command transaction 应用；
- Skill 无法执行任意 shell、读取密钥或访问工程外文件。

## 完成任务后的回写模板

```markdown
### 完成记录

- 日期：YYYY-MM-DD
- 提交：<commit>
- 实现：
- 验证命令：
- 已知限制：
- 后续建议：
```
